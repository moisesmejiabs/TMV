# Local development only. Copy to .dev.vars and use a unique non-production
# value. Never commit .dev.vars or reuse the production secret.
JWT_SECRET=replace-with-a-local-development-secret

# Dedicated Android gateway credential. It must be different from JWT_SECRET.
SMS_GATEWAY_TOKEN=replace-with-a-unique-local-gateway-token
